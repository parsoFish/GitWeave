"""Tests for {{ service_name }}.main."""

import {{ service_name }}.main as main_module


def test_main_is_callable():
    """main() must be importable and callable without raising."""
    main_module.main()
